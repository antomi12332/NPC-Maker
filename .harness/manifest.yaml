apiVersion: apps/v1
kind: Deployment
metadata:
  name: npc-maker
spec:
  replicas: 3
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: npc-maker
    spec:
      containers:
      - image: antomi12332/npc-maker
        name: npc-maker
        ports:
        - containerPort: 3000
